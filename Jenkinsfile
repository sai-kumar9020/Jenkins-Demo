pipeline {
    agent any
    options{
        timestamps()
    }
    stages{
        stage("Stage 1"){
            steps{
                echo "Start timestamps"
            }
        }
        stage("Stage 2"){
            steps{
                echo "end TimeStamps"
            }
        }
    }
}
